# A股智能分析系统 - 前端

现代化的股票分析前端界面，基于 Next.js 14 + TypeScript + Tailwind CSS 构建。

## 功能特性

- ✨ 现代化、优雅、简洁的界面设计
- 📊 实时分析进度显示
- 🤖 多代理协作可视化
- 📱 响应式布局，支持移动端
- 🌙 深色模式支持
- 🎯 个股雷达（预留功能）

## 技术栈

- **框架**: Next.js 15 (App Router)
- **语言**: TypeScript 5.7
- **样式**: Tailwind CSS 3.4
- **动画**: Framer Motion 11
- **图标**: Lucide React

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

前端将在 http://localhost:3000 启动

### 3. 启动后端服务

在项目根目录运行：

```bash
python api.py
```

后端 API 将在 http://localhost:5000 启动

## 项目结构

```
frontend/
├── src/
│   ├── app/              # Next.js App Router 页面
│   │   ├── page.tsx      # 主页面（个股分析 + 个股雷达切换）
│   │   ├── layout.tsx    # 根布局
│   │   └── globals.css   # 全局样式
│   └── components/       # React 组件
│       ├── StockAnalyzer.tsx  # 股票分析组件
│       └── StockRadar.tsx     # 个股雷达组件（预留）
├── public/               # 静态资源
├── package.json          # 项目配置
├── tsconfig.json         # TypeScript 配置
├── tailwind.config.js    # Tailwind 配置
└── next.config.js        # Next.js 配置
```

## 核心功能

### 个股分析

- 输入股票代码（如 002015, 600519）
- 实时显示四个代理的工作状态：
  - 主分析师
  - 技术分析师
  - 基本面分析师
  - 情绪分析师
- 展示完整分析报告

### 个股雷达（预留）

分两大区展示市场异动信号：

**上涨信号区**：
- 创新高
- 连续上涨
- 持续放量
- 向上突破
- 量价齐升

**下跌信号区**：
- 创新低
- 连续下跌
- 持续缩量
- 向下突破
- 量价齐跌

## API 集成

前端通过 Server-Sent Events (SSE) 实时接收后端分析进度：

```typescript
const eventSource = new EventSource(
  `http://localhost:5000/api/analyze?stock_code=${stockCode}`
);

eventSource.onmessage = (event) => {
  const data = JSON.parse(event.data);
  // 处理进度更新
};
```

## 构建部署

### 开发环境

```bash
npm run dev
```

### 生产构建

```bash
npm run build
npm start
```

## 环境要求

- Node.js >= 20.0.0
- npm >= 10.0.0

## 样式定制

编辑 `tailwind.config.js` 自定义主题颜色：

```javascript
theme: {
  extend: {
    colors: {
      primary: { /* 自定义蓝色系 */ },
      success: { /* 自定义绿色系 */ },
      danger: { /* 自定义红色系 */ },
    },
  },
}
```

## 开发说明

- 所有组件使用 TypeScript 严格模式
- 遵循 Next.js 13+ App Router 最佳实践
- 使用 `'use client'` 指令标记客户端组件
- 优先使用 Tailwind 工具类而非自定义 CSS

## License

MIT
